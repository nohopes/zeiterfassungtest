.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
build/
*.iml
*.log

# web/ wird per `flutter create --platforms=web .` erzeugt/verwaltet
# (siehe Dockerfile) - bewusst NICHT ignoriert, da die von uns angepassten
# Dateien (index.html, manifest.json, push/) versioniert werden müssen.

.DS_Store
